type ReportRow = {
    date: number | null,
    sum: number,
    description: string,
    tags: string[]
}

type Operation = {
    date: number,
    description: string,
    sum: number,
    tags: string[]
}

