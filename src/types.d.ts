type ReportRow = {
    date: Date | null,
    sum: number,
    description: string,
    tags: string[]
}

type Operation = {
    date: Date,
    description: string,
    sum: number,
    tags: string[]
}

